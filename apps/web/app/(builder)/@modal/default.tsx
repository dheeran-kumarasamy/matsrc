// Parallel route slot default: renders nothing when there is no intercepted
// route active, so the @modal slot is invisible during normal navigation.
export default function Default() {
  return null;
}
