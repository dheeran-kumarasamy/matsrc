# Phase 6G — Core Platform Readiness Matrix

Read-only-audit-derived matrix. Evidence column references actual files/tests
inspected during this phase, not assumptions. See
`docs/pricing/phase-6g-core-platform-readiness-report.md` for narrative detail.

| Capability | Status | Evidence | Remaining Risk |
|---|---|---|---|
| Extraction abstraction | COMPLETE (for in-use methods) | `pricing-ingestion.service.ts` dispatches per-URL via `hasNativeParserForUrl()` to `NativeHttpExtractorClient`, else `ApifyActorClient`. `scrapeMethod` enum exists (`APIFY_ACTOR/APIFY_CHEERIO/APIFY_PLAYWRIGHT/PDF_PARSE/HTTP_JSON/INTERNAL_QUERY`) but ingestion does not branch on it directly — dispatch is by proven-hostname, not by declared `scrapeMethod`. | `scrapeMethod` is descriptive metadata, not yet the actual dispatch key. Acceptable per spec §5 ("route to correct extractor", not "implement every enum value") since no PLAYWRIGHT/PDF source is approved-enabled. |
| Native HTTP | COMPLETE | `native-http-extractor-client.ts` + 11 passing tests (`native-http-extractor-client.spec.ts`). Only JINDAL_PANTHER/AGNI_STEELS registered — explicit `FAILED` for unregistered hostnames (no silent 0-item success). | None material. |
| Apify | COMPLETE (stub/live split) | `apify-actor-client.ts` — `StubApifyActorClient`/`LiveApifyActorClient` behind `APIFY_ACTOR_CLIENT` DI token, gated by `PricingConfigService.isApifyLiveEnabled()`. | Live actor proven to return 0 items for several sources (documented in Phase 6E docs) — a data-availability gap, not an architecture gap. |
| Playwright architecture | MISSING (by design, undeferred sources only) | `scrapeMethod=APIFY_PLAYWRIGHT` exists in schema/seed for TNSAND/GEM_PORTAL/MSTC_ECOMMERCE/PORTER_LOGISTICS/BLACKBUCK_LOGISTICS, but no Playwright extractor client exists. | No currently-approved production source requires it (all PLAYWRIGHT-tagged sources are unproven/disabled). Out of scope per spec §5. |
| PDF architecture | MISSING (by design) | `scrapeMethod=PDF_PARSE` exists for SAIL/TN_PWD_SOR/TN_TWAD_SOR/TN_DES_BCCI; no PDF extraction code path exists (documented gap, `pricing-ingestion.service.ts` comment lines 296-304). | Same as above — no approved source requires it yet. |
| Raw ingestion | COMPLETE | `PricingRawObservation` preserves source/endpoint/run/raw-* fields/dedupeHash/parseStatus/`rawGeographyLevel` (Phase 6F). `pricing-ingestion.service.spec.ts` (8 tests, incl. 2 new Phase 6G tests for PARTIAL vs SUCCEEDED classification). | None material. |
| Idempotency | COMPLETE | `dedupeHash` (sha256 over source/url/raw-* fields) enforced via a unique DB constraint + `findUnique` pre-check before create; verified by test ("lands only non-duplicate items ... idempotent re-run") and `packages/db/scripts/verify-pricing-dedupe-hash.js`. | None material. |
| Normalization | COMPLETE (documented gaps) | `pricing-normalization.service.ts` + 11 tests. Rejects missing sku/price, quarantines ambiguous/unresolvable unit conversions, never guesses a factor. Documented gap (pre-existing, not fixed this phase per no-unrelated-refactor rule): RANGE/ON_REQUEST price-text detection not implemented — asserted as current behavior in tests, not silently "fixed". | Documented technical debt (implementation-inventory.md #3), unchanged this phase. |
| Canonical SKU | COMPLETE | `PricingCanonicalSku.fingerprint` (deterministic hash over materialCategory/brand/grade/size/pack — verified by `verify-pricing-fingerprint.js`) is geography-independent; `PricingSkuAlias` resolves raw labels to SKUs without any geography input. Confirmed: no code path creates a new canonical SKU based on source/district/state/geographyLevel. | None material. |
| District pricing | COMPLETE | Phase 6F: `geographyLevel=DISTRICT` requires non-null `stateId`+`districtId`, enforced by DB CHECK constraint. | None material. |
| State pricing | COMPLETE | Phase 6F: `geographyLevel=STATE` requires non-null `stateId`, null `districtId`, enforced by DB CHECK constraint. | None material (AGNI remains the only real-world STATE candidate, still disabled). |
| National pricing | COMPLETE (architecturally; no live data) | `geographyLevel=NATIONAL` requires both null, enforced by DB CHECK constraint; `PricingResolutionService` and rollups fully support it. | No NATIONAL-level source exists in production yet — untested against live data (only synthetic fixtures), acceptable per "no live production ingestion" constraint. |
| Price resolution | COMPLETE | `PricingResolutionService.resolveBestAvailablePrice()` — DISTRICT→STATE→NATIONAL, `publicDisplayAllowed`+72h-freshness gate at every level, returns `geographyLevel/state/district/requestedDistrict/fallbackUsed/fallbackReason/confidence/method/asOf/isStale`. 10 passing tests covering all required scenarios (spec §36 Tests 1-10 equivalent). | None material. |
| Daily rollup | COMPLETE | `pricing-daily-rollup.service.ts` groups by `(canonicalSkuId, geographyLevel, stateId, districtId)` via `geoKey`; DISTRICT/STATE/NATIONAL never merged. 8 passing tests incl. 2 Phase 6F geography-isolation tests. | None material. |
| Monthly rollup | COMPLETE | `pricing-monthly-rollup.service.ts` — same `geoKey`-based grouping; MoM/YoY only compare within the same `geoKey`. 6 passing tests incl. 1 Phase 6F geography-isolation test. | None material. |
| Anomaly detection | **FIXED THIS PHASE** | Previously grouped MAD-outlier detection by `districtId` alone — unsafe now that `districtId` is nullable for STATE/NATIONAL (would silently merge different states'/levels' series). Fixed to group by `(canonicalSkuId, geographyLevel, stateId, districtId)`, mirroring the rollups. 12 passing tests incl. 2 new Phase 6G geographic-isolation tests. | None material after fix. |
| Alerts | COMPLETE | `PricingAlertEvaluationService` resolves DISTRICT first, STATE fallback second; persists `geographyLevel`/`stateId` on every evaluation row; notification copy discloses "(state reference)" for a fallback. 9 passing tests. | NATIONAL fallback not wired into the alert engine (documented deferral). |
| Watchlist | COMPLETE | `watchlist-bridge.service.ts` (NestJS) + `watchlist-pricing.ts` (Next.js) both resolve canonical SKU → district → DISTRICT price → STATE fallback, surfacing `geographyLevel`/`isGeographyFallback`/`geographyStateName`. 7 passing tests. | Known duplicate-logic flag (NestJS/Next.js reimplement the same resolution) — documented technical debt, not consolidated. |
| Builder | COMPLETE | District-pricing panel shows `GeographyLevelBadge` + explicit fallback banner; District-Wise report route explicitly scoped to `geographyLevel=DISTRICT` (never silently maps a STATE/NATIONAL row onto a district). 23 passing tests. | None material. |
| Supplier | COMPLETE | `market-intelligence-data.ts`/`supplier-data.ts` explicitly filter `geographyLevel=DISTRICT` for district-scoped views (Phase 6F, re-verified this phase); INTERNAL_ONLY sources never surfaced. 31 passing tests. | None material. |
| Admin | PARTIALLY COMPLETE / DEFERRED (test tooling) | `AdminPricingController`/`PricingAdminOpsService` (848 lines) cover dashboard/sources/endpoints/scheduler/rollups/anomalies/canonical-SKU/alias/unmapped-queue, RBAC-gated + audit-logged. 20 passing backend tests. **No automated UI test suite exists for `apps/admin`** — verified via API-contract/backend-service inspection only, per instruction not to introduce new test tooling this phase. | Admin UI React components have zero automated coverage — documented, not fabricated as tested. |
| Audit | COMPLETE | `AuditService`/`AuditController` (RBAC-gated) read `AuditLog`; `PricingAdminOpsService` writes an audit row for every mutating action (source/endpoint status, ingest/rollup triggers, SKU merge/rename, alias/unmapped-queue actions, anomaly resolve/comment, scheduler pause/resume) — 20+ verified call sites. | None material. |
| Compliance | COMPLETE | Resolution/rollups/public API/Builder/watchlist/alerts all gate on `publicDisplayAllowed`; `updateSourceStatus()` refuses to enable a source without `tosReviewedAt`+`robotsAllowed`+complete config, audit-logged; `verify-pricing-source-compliance.js` enforces the same invariants at the data level. | 33 pre-existing compliance violations remain in seed data (unrelated to this phase, not touched). |
| Scheduler | COMPLETE | `PricingSchedulerService` — daily cron (anomaly detection → daily rollup → alert evaluation, each independently try/caught) + separate monthly cron, both gated by feature flag. | No dedicated orchestration test file (constituent services are tested individually) — acceptable, thin wiring with documented try/catch boundaries. |
| Observability | COMPLETE (existing fields, not duplicated) | `PricingScrapeRun` (fetched/landed/duplicate/status/timestamps); `getDashboardSummary()` exposes platformHealth/processingSummary/observationTrend/pipelineStatus from existing fields. | None material. |
| Data quality | COMPLETE | `DataQualityDashboardPanel` + backend summary distinguish coverage/staleness/compliance-blocked states. | Not independently re-verified against a live endpoint this phase (read-only audit); no defect found. |
| Security | COMPLETE | Every admin pricing route: `OptionalJwtAuthGuard`+`RoleGuard`+`@Roles("ADMIN")`. Public pricing routes intentionally unauthenticated (public data, gated by `publicDisplayAllowed`). No guard weakened this phase. | None material. |
| Database safety | COMPLETE (Phase 6F-1, preserved) | `packages/db/lib/db-safety*.js` + `scripts/prisma-safe.js`/`db-identity.js`/`verify-db-safety.js` unchanged; re-run and passing this phase. | None — infrastructure only, not redesigned. |
| Tests | COMPLETE (gap closed) | 274/275 `apps/api` tests passing (+4 net new: 2 anomaly-isolation, 2 ingestion PARTIAL/SUCCEEDED); 61/61 `apps/web`; 31/31 `apps/supplier`; 1 pre-existing unrelated WhatsApp failure. | Admin UI test gap remains (documented, not closed per explicit instruction). |
